{
  "name": "myapp",
  "private": "true",
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --outdir=app/assets/builds"
  },
  "dependencies": {
    "esbuild": "^0.17.18"
  },
  "devDependencies": {
    "postcss-lit": "^1.1.0",
    "stylelint": "^15.6.1",
    "stylelint-config-standard": "^33.0.0"
  }
}
